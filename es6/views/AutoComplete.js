import * as React from 'react';
import { BasicComponent } from './BasicComponent';
export class AutoComplete extends BasicComponent {
    constructor(props) {
        super(props);
    }
    render() {
        let props = this.props;
        return (React.createElement("div", {className: "react-ui-autocomplete"}, React.createElement("input", {type: "text", placeholder: }), ");" + ' ' + "}" + ' ' + "}"));
    }
}
